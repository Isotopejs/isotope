import { createUtil } from "../util";
const bgAuto = createUtil({ backgroundSize: "auto" });
const bgCover = createUtil({ backgroundSize: "cover" });
const bgContain = createUtil({ backgroundSize: "contain" });
export { bgAuto, bgCover, bgContain };
//# sourceMappingURL=background-size.js.map