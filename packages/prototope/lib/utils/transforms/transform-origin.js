import { createUtil } from "../util";
const originCenter = createUtil({ transformOrigin: "center" });
const originTop = createUtil({ transformOrigin: "top" });
const originTopRight = createUtil({ transformOrigin: "top right" });
const originRight = createUtil({ transformOrigin: "right" });
const originBottomRight = createUtil({ transformOrigin: "bottom right" });
const originBottom = createUtil({ transformOrigin: "bottom" });
const originBottomLeft = createUtil({ transformOrigin: "bottom left" });
const originLeft = createUtil({ transformOrigin: "left" });
const originTopLeft = createUtil({ transformOrigin: "top left" });
export { originCenter, originTop, originTopRight, originRight, originBottomRight, originBottom, originBottomLeft, originLeft, originTopLeft };
//# sourceMappingURL=transform-origin.js.map