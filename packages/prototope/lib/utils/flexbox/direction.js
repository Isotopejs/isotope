import { createUtil } from "../util";
const flexRow = createUtil({ flexDirection: "row" });
const flexRowReverse = createUtil({ flexDirection: "row-reverse" });
const flexCol = createUtil({ flexDirection: "column" });
const flexColReverse = createUtil({ flexDirection: "column-reverse" });
export { flexRow, flexRowReverse, flexCol, flexColReverse };
//# sourceMappingURL=direction.js.map