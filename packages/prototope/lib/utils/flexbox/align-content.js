import { createUtil } from "../util";
const contentStart = createUtil({ alignContent: "flex-start" });
const contentCenter = createUtil({ alignContent: "center" });
const contentEnd = createUtil({ alignContent: "flex-end" });
const contentBetween = createUtil({ alignContent: "space-between" });
const contentAround = createUtil({ alignContent: "space-around" });
export { contentStart, contentCenter, contentEnd, contentBetween, contentAround };
//# sourceMappingURL=align-content.js.map