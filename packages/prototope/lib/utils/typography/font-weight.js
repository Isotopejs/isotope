import { createUtil } from "../util";
const fontHairline = createUtil({ fontWeight: "100" });
const fontThin = createUtil({ fontWeight: "200" });
const fontLight = createUtil({ fontWeight: "300" });
const fontNormal = createUtil({ fontWeight: "400" });
const fontMedium = createUtil({ fontWeight: "500" });
const fontSemibold = createUtil({ fontWeight: "600" });
const fontBold = createUtil({ fontWeight: "700" });
const fontExtrabold = createUtil({ fontWeight: "800" });
const fontBlack = createUtil({ fontWeight: "900" });
export { fontHairline, fontThin, fontLight, fontNormal, fontMedium, fontSemibold, fontBold, fontExtrabold, fontBlack };
//# sourceMappingURL=font-weight.js.map