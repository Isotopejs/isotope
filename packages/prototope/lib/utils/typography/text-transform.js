import { createUtil } from "../util";
const uppercase = createUtil({ textTransform: "uppercase" });
const lowercase = createUtil({ textTransform: "lowercase" });
const capitalize = createUtil({ textTransform: "capitalize" });
const normalCase = createUtil({ textTransform: "none" });
export { uppercase, lowercase, capitalize, normalCase };
//# sourceMappingURL=text-transform.js.map