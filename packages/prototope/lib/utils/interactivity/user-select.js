import { createUtil } from "../util";
const selectNone = createUtil({ userSelect: "none" });
const selectText = createUtil({ userSelect: "text" });
const selectAll = createUtil({ userSelect: "all" });
const selectAuto = createUtil({ userSelect: "auto" });
export { selectNone, selectText, selectAll, selectAuto };
//# sourceMappingURL=user-select.js.map