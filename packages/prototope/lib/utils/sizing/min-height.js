import { createUtil } from "../util";
const minH0 = createUtil({ minHeight: "0" });
const minHFull = createUtil({ minHeight: "100%" });
const minHScreen = createUtil({ minHeight: "100vh" });
export { minH0, minHFull, minHScreen };
//# sourceMappingURL=min-height.js.map