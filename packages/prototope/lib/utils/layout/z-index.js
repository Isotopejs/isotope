import { createUtil } from "../util";
/**
 * Prototope z-index  util.
 *
 * @param z - Z index.
 * @returns - Prototope util.
 */
const z = (z) => createUtil({ zIndex: `${z}` });
export { z };
//# sourceMappingURL=z-index.js.map