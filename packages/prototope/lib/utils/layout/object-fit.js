import { createUtil } from "../util";
const objectContain = createUtil({ objectFit: "contain" });
const objectCover = createUtil({ objectFit: "cover" });
const objectFill = createUtil({ objectFit: "fill" });
const objectNone = createUtil({ objectFit: "none" });
const objectScaleDown = createUtil({ objectFit: "scale-down" });
export { objectContain, objectCover, objectFill, objectNone, objectScaleDown };
//# sourceMappingURL=object-fit.js.map