import { createUtil } from "../util";
const overflowAuto = createUtil({ overflow: "auto" });
const overflowHidden = createUtil({ overflow: "hidden" });
const overflowVisible = createUtil({ overflow: "visible" });
const overflowScroll = createUtil({ overflow: "scroll" });
const overflowXAuto = createUtil({ overflowX: "auto" });
const overflowYAuto = createUtil({ overflowY: "auto" });
const overflowXHidden = createUtil({ overflowX: "hidden" });
const overflowYHidden = createUtil({ overflowY: "hidden" });
const overflowXVisible = createUtil({ overflowX: "visible" });
const overflowYVisible = createUtil({ overflowY: "visible" });
const overflowXScroll = createUtil({ overflowX: "scroll" });
const overflowYScroll = createUtil({ overflowY: "scroll" });
const scrollingTouch = createUtil({ WebkitOverflowScrolling: "touch" });
const scrollingAuto = createUtil({ WebkitOverflowScrolling: "auto" });
export { overflowAuto, overflowHidden, overflowVisible, overflowScroll, overflowXAuto, overflowYAuto, overflowXHidden, overflowYHidden, overflowXVisible, overflowYVisible, overflowXScroll, overflowYScroll, scrollingTouch, scrollingAuto };
//# sourceMappingURL=overflow.js.map