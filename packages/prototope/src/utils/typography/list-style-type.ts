import { createUtil } from "../util";

const listNone = createUtil({ listStyleType: "none" });
const listDisc = createUtil({ listStyleType: "disc" });
const listDecimal = createUtil({ listStyleType: "decimal" });

export { listNone, listDisc, listDecimal };
