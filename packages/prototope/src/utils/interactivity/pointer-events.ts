import { createUtil } from "../util";

const pointerEventsNone = createUtil({ pointerEvents: "none" });
const pointerEventsAuto = createUtil({ pointerEvents: "auto" });

export { pointerEventsNone, pointerEventsAuto };
